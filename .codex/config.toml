# =============================================================================
# Project Codex config (this file lives inside the repo)
# Loaded only when the project/workspace is trusted.
# =============================================================================

approval_policy = "on-request"
# sandbox_mode = "workspace-write"
sandbox_mode = "danger-full-access"
